import { rawListeners } from "process";
import React from "react"; 

export const boxSize=264

export const mainBoardStyle : React.CSSProperties={
    display:"flex",
    flexDirection: "row",
    justifyContent:"center",
    alignItems:"center"

}