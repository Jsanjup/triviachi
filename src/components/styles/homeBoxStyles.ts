import React from "react";

export const homeBoxStyle : React.CSSProperties = {
    display:"flex",
    justifyContent:"center",
    alignItems:"center",
    flexDirection: "column",
}

export const basesRowStyle : React.CSSProperties = {
    display:"flex", 
    flexDirection:"row"
}