import React from "react";

export const squareBoxStyle : React.CSSProperties = {
    display:"flex",
    justifyContent:"center",
    alignItems:"center",
    flexDirection: "row",
}

export const squareBoxColumnStyle : React.CSSProperties = {
    display:"flex", 
    flexDirection:"column"
}